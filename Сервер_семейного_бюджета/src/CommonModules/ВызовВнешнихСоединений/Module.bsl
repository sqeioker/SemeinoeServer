
Функция Синхронизация(СтрокаСообщения) Экспорт
	
	Если СтрокаСообщения = "ЭтоПервоеПодключение" Тогда
	   ОтветКлиенту = ПолучениеДанных.ПерваяВыгрузка(СтрокаСообщения);
	Иначе	
	   ОтветКлиенту = ПолучениеДанных.ЗарегистрироватьВыгрузку(СтрокаСообщения);
	КонецЕсли;
	
	Возврат ОтветКлиенту;

КонецФункции 
